each event in events
	.event_item
		.event_poster(style='background-image: url(' + event.poster + ')')
		.event_body
			.event_head
				.event_date= moment(event.schedule.date).locale(locale).format('D MMMM')
				.event_ticket= __('купить билет')
			.event_title= get_locale(event.title, locale)
			.event_meta
				.event_age= event.age
				.event_members
					each group in event.members
						.members_group
							.group_title= get_locale(group.title, locale)
							.group_items
								each member in group.list
									a.member_item(href='#')= get_locale(member.name, locale)
			.event_description= get_locale(event.s_title, locale)